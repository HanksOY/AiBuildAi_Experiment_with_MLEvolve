# Copy to .env and fill in. .env is git-ignored; this example file is not.
# Existing exported environment variables take precedence over .env.

# LLM API keys, referenced from config/config.yaml via ${oc.env:...}
MLEVOLVE_CODE_API_KEY=
MLEVOLVE_FEEDBACK_API_KEY=
